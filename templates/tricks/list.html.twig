<div class="btn-group flex flex-wrap" role="group" aria-label="Basic example">
	{% if tricks|length > 0 %}
		{% for trick in tricks %}
			<div class="card m-2 mt-4" style="width: 18rem;">
				<img class="card-img-top" style="height: 200px; background-color: grey;" src="" alt="Card image cap">
				<div class="card-body">
					<span class="badge bg-secondary" style="width: fit-content">{{ trick.category.name}}</span>
					<h5 class="card-title">{{ trick.name }}</h5>
					<p class="card-text">{{ trick.description }}</p>
					<a href="{{ path('app_tricks_details', {"slug": trick.slug}) }}" class="btn btn-primary">Voir la figure</a>
					<a href="#" class="btn btn-secondary"><i class="fa-solid fa-pen"></i></a>
					<a href="#" class="btn btn-danger"><i class="fa-solid fa-trash"></i></a>
				</div>
			</div>
		{% endfor %}
	{% else %}
		<div class="mt-4">
			<p>Il n'y a aucune figure dans cette catégorie.</p>
		</div>
	{% endif %}
</div>
